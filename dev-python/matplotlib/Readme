I “reinstalled” it again with the same command, plus one flag.

python setup.py install --record files.txt
cat files.txt | xargs rm -rf


git clone git://github.com/matplotlib/matplotlib.git
cd matplotlib
python setup.py install
